ѕодвиг 3. ѕродолжите программу. ќбъ€вите указатель ptr_a на переменную a и указатель ptr_b на 
переменную b. ѕомен€йте адреса указателей ptr_a и ptr_b между собой, использу€ 
вспомогательный указатель с обобщенным типом void*. ¬ыведите в консоль в одну строчку через 
пробел целочисленные значени€, на которые ссылаютс€ указатели ptr_a и ptr_b именно в таком пор€дке:
<значение дл€ ptr_a> <значение дл€ ptr_b>

#include <stdio.h>
int main(void)
{
    short a, b;
    short *ptr_a, *ptr_b; // ќбъ€вление указателей
    void *swap_ptr; // ¬спомогательный указатель с обобщенным типом

    scanf("%hd, %hd", &a, &b);

    ptr_a = &a; // ѕрисваивание указател€м адресов
    ptr_b = &b;
    swap_ptr = ptr_a; // присванивание указател€ во временный указатель
    ptr_a = ptr_b;
    ptr_b = swap_ptr; // ќбмен адресов 

    printf("%d %d", *ptr_a, *ptr_b);

    __ASSERT_TESTS__ // макроопределение дл€ тестировани€ (не убирать и должно идти непосредственно перед return 0)
    return 0;
}



